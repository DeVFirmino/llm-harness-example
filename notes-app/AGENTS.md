# AGENTS.md

Read `facts.md` before proposing a change.
Treat it as canonical and point out conflicts instead of inventing around them.
When the user corrects a project assumption, propose an update to `facts.md` and show the diff.
